import { defineMessages } from "react-intl";

export default defineMessages({
  noResultsText: {
    id: "Search.SearchBarList.noResultsText",
    defaultMessage: "Sorry... No results for your query.",
  },
});
