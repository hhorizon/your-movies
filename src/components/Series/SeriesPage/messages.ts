import { defineMessages } from "react-intl";

export default defineMessages({
  title: {
    id: "Series.SeriesPage.title",
    defaultMessage: "All series",
  },
  subtitle: {
    id: "Series.SeriesPage.subTitle",
    defaultMessage: "Series from all over the world",
  },
});
